export default function Loading() {
  return (
    <div className="page-shell">
      <div className="panel p-8 text-center">
        <p className="text-xs uppercase tracking-[0.28em] text-[rgb(var(--accent))]">Loading</p>
        <p className="mt-3 text-sm text-ink/70">Preparing your view...</p>
      </div>
    </div>
  );
}
